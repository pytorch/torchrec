# generated by running
# cd ~/fbsource/fbcode/torchx/fb/example
# torchx configure --all --schedulers local_cwd,mast,flow
#
# these are project defaults, you can override them from cli as
# $ torchx run -s mast -cfg hpcClusterUuid=FooBar,hpcJobOncall=oncall_barbaz

[local_cwd]
log_dir = None
prepend_cwd = False

[mast]
hpcClusterUuid = TSCTestCluster
hpcIdentity = oncall_torchrec
hpcJobOncall = torchrec

[flow]
secure_group = oncall_torchrec
entitlement = default
proxy_workflow_image = None
