# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: CPU Unit Test CI

on:
  push:
    paths-ignore:
      - "docs/*"
      - "third_party/*"
      - .gitignore
      - "*.md"
  pull_request:
    paths-ignore:
      - "docs/*"
      - "third_party/*"
      - .gitignore
      - "*.md"

jobs:
  build_test:
    strategy:
        matrix:
          include:
            - os: linux.2xlarge
              python-version: 3.7
              python-tag: "py37"
              cuda-tag: "cpu"
            - os: linux.2xlarge
              python-version: 3.8
              python-tag: "py38"
              cuda-tag: "cpu"
            - os: linux.2xlarge
              python-version: 3.9
              python-tag: "py39"
              cuda-tag: "cpu"
    uses: pytorch/test-infra/.github/workflows/linux_job.yml@main
    with:
      runner: ${{ matrix.os }}
      timeout: 30
      script: |
        ldd --version
        conda create -yp --name build_binary python=${{ matrix.python-version }}
        conda info
        python --version
        conda activate build_binary
        python --version
        conda install --pre torch --extra-index-url https://download.pytorch.org/whl/nightly/cpu
        conda install -r requirements.txt
        conda uninstall fbgemm_gpu-nightly -y
        conda install fbgemm-gpu-nightly-cpu
        python -c "import torch.distributed"
        echo "torch.distributed succeeded"
        python -c "import skbuild"
        echo "skbuild succeeded"
        python -c "import numpy"
        python -c "import fbgemm_gpu"
        echo "fbgemm_gpu succeeded"
        echo "numpy succeeded"
        python setup.py bdist_wheel \
          --package_name torchrec-test-cpu \
          --python-tag=${{ matrix.python-tag }}
        python -c "import torchrec"
        conda install pytest
        pytest torchrec -v -s -W ignore::pytest.PytestCollectionWarning --continue-on-collection-errors -k 'not test_sharding_gloo_cw'
        echo "Starting C++ Tests"
        conda install -y gxx_linux-64
        x86_64-conda-linux-gnu-g++ --version
        mkdir cpp-build
        cd cpp-build
        cmake \
            -DBUILD_TEST=ON \
            -DCMAKE_PREFIX_PATH=/opt/conda/envs/build_binary/lib/python3.7/site-packages/torch/share/cmake ..
        make -j
        ctest -v .
